import React from "react";
import './style.css'

const Header = () => {
    return (
        <header className="app-header">
            <h2><a href="/">Conversation</a></h2>
        </header>
    );
};

export default Header;
